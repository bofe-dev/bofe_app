query CurrentUserProfile {
    currentUser {
        ...UserFragment
        fullName
        birthdate
        languageCode
        countryCode
        avatarImageAttachment {
            ...AttachmentFragment
        }
    }
}
