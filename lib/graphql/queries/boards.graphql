query Boards($after: UUID, $first: Int) {
    boards(after: $after, first: $first) {
        nodes {
            ...BoardItemFragment
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
